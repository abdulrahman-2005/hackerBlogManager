# from utils import General

# fileManager = General.FileManager

# fileManager.create_folder("blog", "..")
# fileManager.create_folders(["ar", "en"], "./blog")
    
# # setters.save_json(paths["data"], data)